server:
  port: 8090

spring:
  application:
    name: tasklist
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/tasklist?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: djou123